import "@/styles/sidebarItem.css";
import { useEffect, useState } from "react";
import SidebarItemMenu from "./sidebarItemMenu";
import { EventOutput, SportInfoOutput } from "@/lib/types";

interface AdminPastEventProps {
  event: EventOutput;
}

export default function AdminPastEvent({ event }: AdminPastEventProps) {
  return (
    <div>
      <div className="">
          
      </div>
    </div>
  );
}
